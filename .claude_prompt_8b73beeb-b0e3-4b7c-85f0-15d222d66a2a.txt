You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
[PR Feedback] Sync Configuration & Adapter Implementation

**Description**:
Based on consolidated PR review, address the following in **Sync Configuration & Adapter Implementation**:

- **SyncConfig missing cross-field validation allows silent misconfiguration**: `config.py:98` — `SyncConfig` allows `adapter=s3` with `s3=None` or `adapter=duckdb` with `duckdb=None`. No `@model_validator` enforces adapter-specific sub-config presence. App starts successfully but fails at runtime on first sync with an opaque error. Combined with `app.py` passing `s3_access_key or ""` (empty string credentials), S3 adapter initializes as configured but fails every operation.
- **DuckDBSyncAdapter does not use DuckDB**: `adapters/sync/duckdb_sync.py` — Class uses PyArrow exclusively for Parquet I/O; DuckDB is not imported or invoked. Naming contradicts the spec requirement to use "DuckDB + Parquet as the serialization format" and the implementation's own docstring.
- **get_sync_status always reports unprocessed_count=0**: `adapters/sync/duckdb_sync.py:316` and S3 adapter — Both adapters hardcode `unprocessed_count=0` instead of querying `ChangeRepository.count_unprocessed()`. The count is effectively dead data.
- **DuckDBSyncAdapter.pull silently skips directories with unparseable date names**: `adapters/sync/duckdb_sync.py:523-528` — Directories with unexpected name formats are logged and skipped, making their events permanently invisible. Same in `s3_sync.py:212-217`. When `since=None`, all directories should be read regardless of name format.

---
_Created by PR Review Stage - Consolidation Phase_

---

## Concurrent Fix Issues (Same Review Cycle)

The following issues are being fixed simultaneously in this review cycle. Check these issues for any files they modify before making your own changes to avoid introducing conflicts:

- #504: [PR Feedback] Conflict Resolution Logic
- #506: [PR Feedback] API Response Layer
- #507: [PR Feedback] Error Handling in Versioning & Sync
- #508: [PR Feedback] Test Coverage Gaps
- #509: [PR Feedback] Front-End Type Safety

### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git