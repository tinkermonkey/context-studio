# Test-specific requirements with pinned versions
# Resolves pydantic v1/v2 conflicts for Phase 3 testing

# Core testing frameworks
pytest==7.4.4
pytest-asyncio==0.23.4
pytest-cov==4.1.0
pytest-timeout==2.2.0
pytest-mock==3.12.0

# FastAPI and dependencies (pydantic v2 compatible)
fastapi==0.109.0
pydantic==2.6.0
pydantic-settings==2.1.0
uvicorn==0.27.0
starlette==0.35.1

# Database and vector search
sqlalchemy==2.0.25
sqlite-vec==0.1.0
alembic==1.13.1

# HTTP and async
httpx==0.26.0
aiohttp==3.9.3
aiosqlite==0.19.0

# Caching
cachetools==5.3.2

# NLP and embeddings (compatible versions)
spacy==3.7.2
sentence-transformers==2.3.1
numpy==1.26.3
scikit-learn==1.4.0

# Utilities
python-dotenv==1.0.0
