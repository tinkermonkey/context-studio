# 08.1 Great Renaming Requirements

Rename the core platform data elements according to this mapping:

- `Domain` -> `Category`
- `Layer` -> `Domain` (`Facets`?)

Within the `Domain` table, rename the `primary_predicate` field to be `structural_predicate`