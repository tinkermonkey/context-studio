# 13.3 Normalization Phase 2

Normalize the event handling and change tracking.

## 13.3.1 Migration Strategy

This change set should be considered part of the '006_nodes.py' migration, all changes to schemas should be incorporated into that migration set and replace conflicting edits to that file.

- All existing event migration code in '006_nodes.py' should be removed and replaced with the correct migration code for this change set

**No Backwards Compatibility** should be incormporated beyond the basic migration roll-back code.

## 13.3.2 Normalized Event Naming

Coalesce all node eventing to use the name ChangeEvent

  - Rename the `NodeEvent` model to `ChangeEvent`

  - Rename the `structure_node_events` table to `change_events`

  - Rename the `NodeEventHandler` to `ChangeEventHandler`

  - The EventProcessor has method-specific logic (process_layer_event, process_domain_event, etc.) that needs to be generalized for the new record_type approach.

## 13.3.3 Normalized Event Schema

Create a normalized change event schema to support change data capture:

- Rename the current `node_id` field to `record_id`

- Add a field named `record_type` to capture the type of record the change represents

## 13.3.4 Predicate Event Tracking

Add change event tracking for the `predicates` table using the new normalized change events.

```python
# Suggested record_type enumeration:
RECORD_TYPES = [
    "structure_node",     # For layers/domains/terms
    "structure_node_link", # For relationships  
    "predicate"           # For predicate definitions (currently missing!)
]
```

## 13.3.4 Normalize Event Tests

Update all unit and integration tests around events to use the new schema and models