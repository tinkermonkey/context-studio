
You are a Senior Software Engineer.

I implement clean, well thought out code with proper error handling and maintainable architecture.

**Issue Title**: [PR Feedback] E2E Test Helpers

**Description**:
Based on consolidated PR review, address the following in **E2E Test Helpers**:

- **`retry_on_external_failure` Retries All Exceptions Including AssertionError**: `/workspace/local-server/tests/e2e/helpers.py:41-44` — The retry decorator catches bare `Exception`, meaning genuine test failures (`AssertionError`) and programming bugs are silently retried up to 3 times, tripling CI wall-clock time for real failures and masking their true nature.
- **`retry_on_external_failure` Missing `delay` Parameter**: The e2e test strategy specifies `retry_on_external_failure(max_retries=2, delay=5)` with `time.sleep(delay)` between retries. The implementation omits the `delay` parameter entirely and retries immediately with zero delay.
- **`retry_on_external_failure` Raises Instead of Skipping on Exhaustion**: The strategy specifies calling `pytest.skip(...)` after all retries are exhausted to distinguish unavailable services from real failures. The implementation raises `last_exception` instead, reporting a failure rather than a skip.
- **`poll_until` Hangs for Full Timeout on Deterministic Bugs**: `/workspace/local-server/tests/e2e/helpers.py:88-90` — The polling helper catches all `Exception` types during predicate evaluation and continues polling until timeout. Programming errors (`TypeError`, `KeyError`) cause 30-second hangs before surfacing as a misleading `TimeoutError`.

---
_Created by PR Review Stage - Consolidation Phase_

---

## Concurrent Fix Issues (Same Review Cycle)

The following issues are being fixed simultaneously in this review cycle. Check these issues for any files they modify before making your own changes to avoid introducing conflicts:

- #296: [PR Feedback] Reference Manager
- #298: [PR Feedback] E2E Test Fixtures (conftest.py)
- #299: [PR Feedback] SQL Builder Documentation
- #300: [PR Feedback] Integration Test Cleanup
- #301: [PR Feedback] CI/CD Configuration
- #302: [PR Feedback] E2E Test Assertions



