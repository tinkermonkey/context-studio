You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
Phase 6D: E2E Tests and Finalization

**Description**:
Author the four admin end-to-end tests from `e2e_test_strategy.md` §Test Phase 4, regenerate the OpenAPI spec, and verify domain purity and full test suite health.

## Requirements
- Create `tests/e2e/test_phase4_admin.py` containing exactly these four tests:\n  - `test_health_check_reports_real_status`: asserts `database_connected` is `true` and per-service statuses reflect real adapter state\n  - `test_system_metrics_accuracy`: asserts service-level metrics (creation counts, database pool state, embedding model readiness) match actual system state\n  - `test_configuration_read_and_update`: modifies a non-credential setting, simulates a server restart cycle, and confirms the value persists\n  - `test_background_task_lifecycle`: verifies `PENDING → RUNNING → COMPLETED` state transitions including `started_at`, `completed_at`, and `result` fields\n- Run `scripts/update_api_specs.py` and commit the updated `openapi.json`\n- Run `scripts/check_domain_imports.py` and confirm no violations\n- Run the full test suite: `pytest tests/unit/ tests/integration/ tests/e2e/test_phase4_admin.py`

## Design Guidance
Implements Phase 6D (E2E Tests and Finalization) from the architecture design. Tests are specified in `e2e_test_strategy.md` §Test Phase 4.

## Acceptance Criteria
- [ ] `tests/e2e/test_phase4_admin.py` exists and contains the 4 required test functions\n- [ ] All 4 e2e tests pass with `pytest tests/e2e/test_phase4_admin.py` against a running server with real SQLite databases\n- [ ] `test_configuration_read_and_update` confirms persistence across a restart cycle\n- [ ] `test_background_task_lifecycle` validates `PENDING → RUNNING → COMPLETED` transitions with accurate timestamps\n- [ ] `openapi.json` reflects all 6 new endpoints\n- [ ] `scripts/check_domain_imports.py` reports zero violations\n- [ ] Full test suite passes with no regressions

## Dependencies
Phase 6C: Web Layer Updates

## Parent Issue
Part of #335


### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git