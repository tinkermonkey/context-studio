openapi:
  openapi: 3.0.3
  info:
    title: Delete Node
    version: 1.0.0
  paths:
    /api/structure_nodes/{node_id}:
      delete:
        operationId: delete_node_api_structure_nodes__node_id__delete
        summary: Delete Node
        description: 'Delete a structure_node and its children.


          This operation cascades to all child structure_nodes and their relationships.

          Use with caution as this operation cannot be undone.'
        parameters:
        - name: node_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
            description: The ID of the structure_node to delete
            title: Node Id
          description: The ID of the structure_node to delete
        - name: SessionLocal
          in: query
          required: false
          schema:
            title: Sessionlocal
        requestBody: null
        responses:
          '204':
            description: Successful Response
          '422':
            description: Validation Error
            content:
              application/json:
                schema:
                  $ref: '#/components/schemas/HTTPValidationError'
        tags:
        - structure_nodes
        - structure_nodes
