I've created a comprehensive agent definition for `context-studio-api-expert`. The agent is grounded in actual codebase examples including:

- **Real route handlers** from `ontology_routes.py` and `extraction_routes.py`
- **Actual Pydantic schemas** like `TaxonomyCreateRequest` 
- **Real dependency injection patterns** from `dependencies.py` and `app.py`
- **Actual error handling** from `_handle_domain_error()`

The agent covers FastAPI async patterns, OpenAPI contract validation, hexagonal DI, and domain exception mapping with concrete examples from the project.