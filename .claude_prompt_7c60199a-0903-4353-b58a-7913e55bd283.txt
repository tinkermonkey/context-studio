You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
[PR Feedback] DuckDB Sync Adapter

**Description**:
Based on consolidated PR review, address the following in **DuckDB Sync Adapter**:

- **Silent JSON parse failures replace data with empty dicts**: In `pull()`, `json.JSONDecodeError` and `TypeError` on `new_state`/`previous_state` deserialization are caught and silently swallowed — `new_state` becomes `{}` and `previous_state` becomes `None` with zero logging. This violates the adapter's own fail-fast contract and causes silent data corruption during merge. `/workspace/local-server/adapters/sync/duckdb_sync.py:218-228`
- **Module docstrings and descriptions are factually wrong — DuckDB is never used**: The module docstring, class docstring, and Raises sections reference "DuckDB operations" but `duckdb` is never imported or called. All I/O uses `pyarrow`/`pyarrow.parquet`. The `duckdb` importability check in `__init__` is dead validation. `/workspace/local-server/adapters/sync/duckdb_sync.py:1-12,32,51,90`
- **`pull()` does not filter events by timestamp within a date partition**: Filters directories by date but not individual events by timestamp. If `since` is mid-day, all events from that entire date are returned, causing duplicate processing. `/workspace/local-server/adapters/sync/duckdb_sync.py:202-212`
- **`is_configured()` swallows OSError without logging**: Returns `False` on `OSError` without any log, causing sync status to report "not configured" when the real problem is filesystem failure. `/workspace/local-server/adapters/sync/duckdb_sync.py:289-292`
- **Broad `except Exception` wraps programming bugs as RuntimeError**: Catches `KeyError`, `AttributeError`, etc. and wraps them as generic `RuntimeError`. The `errors` list declared at ~line 97 is dead code (never populated). `/workspace/local-server/adapters/sync/duckdb_sync.py:158-161`

---
_Created by PR Review Stage - Consolidation Phase_

---

## Concurrent Fix Issues (Same Review Cycle)

The following issues are being fixed simultaneously in this review cycle. Check these issues for any files they modify before making your own changes to avoid introducing conflicts:

- #497: [PR Feedback] Ontology Change Event Recording
- #498: [PR Feedback] Versioning Service — Auto-Resolve & Merge Logic
- #500: [PR Feedback] Sync Configuration & Adapter Wiring
- #501: [PR Feedback] Missing Spec-Required Features
- #502: [PR Feedback] Test Coverage Gaps
- #503: [PR Feedback] Frontend Types

### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git